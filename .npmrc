# backbone.marionette 3.x pins underscore ~1.8.3 as a peer dependency, but the
# script-tag bundle runs fine against current underscore (the previously vendored
# copy was 1.13.7). npm is only used to source dist files here, not to resolve a
# module graph, so relax peer enforcement.
legacy-peer-deps=true
